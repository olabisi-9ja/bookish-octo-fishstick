/**
 * Figma 162:1076 - "rider homepage".
 * Route exists and navigates; the screen itself is not drawn from Figma yet.
 */
import { PendingScreen } from '@/components';

export default function HomeResults() {
  return (
    <PendingScreen
      title="rider homepage"
      node="162:1076"
      next="/(rider)/datetime"
    />
  );
}
